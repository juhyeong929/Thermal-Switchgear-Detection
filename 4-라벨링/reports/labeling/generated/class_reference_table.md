# 가공 대상 종합표 (재생성)

> **이 파일은 손으로 고치지 않는다.** `scripts/build_reference_table.py` 가
> `schemas/classes_v2.py` 에서 만든다. 스키마를 고치고 다시 돌린다.

## 이 표가 왜 여기 있는가

이전 발주처 가이드(HTML)의 `03 REFERENCE — 가공 대상 종합` 표가
**2026-09-02 배포판(PDF)에서 없어졌다.** 새 판은 반별 가공대상 목록만 두고
클래스 번호·가공여부 기호·근거 열을 두지 않는다.

그 결과 **현행 배포 가이드만으로는 다음을 설명할 수 없다.**

```
· class_id 가 왜 그 번호인가            (가이드번호 - 1 의 기계적 변환)
· 부스바 · 케이블 · ACB 접촉부가 왜 제외인가
```

근거 문장 자체는 이전 판에서 `classes_v2.py` 의 `description` 으로 옮겨져 있다.
아래 표는 그것을 사람이 읽을 형태로 되살린 것이다.

```
현행 배포 가이드   수배전반 라벨링 가이드.pdf
이전 판(종합표 원문 출처)  수배전반_열화상_라벨링_가이드_v2.html
                   워크트리에서 삭제됨 — git 이력에서만 열람 가능
단일 출처          schemas/classes_v2.py
```

## 상태 기호

| 기호 | 상태 | 뜻 |
|---|---|---|
| `O` | 가공 | 항상 라벨링 |
| `!` | 주의 | 식별 가능할 때만 라벨. 판별 불가면 미라벨/Ignore |
| `X` | 제외 | 물체는 있지만 어떤 반에서도 그리지 않는다 |
| `폐지` | 폐지 | 가이드 개정으로 **클래스 자체가 없어졌다.** 신규 라벨 금지 |

**`X`(제외)와 `폐지`는 다르다.** 제외는 "있지만 그리지 않는다", 폐지는
"그 이름의 클래스가 더 이상 없다" 이다. 둘을 한 칸에 합치면 제외 3종의 근거를
설명할 수 없게 되므로 코드에서도 집합을 나눠 둔다
(`EXCLUDED` / `RETIRED_CLASSES`).

## 종합표

| # | id | 클래스 | 상태 | 근거 | 세는 단위 | 출현 반 |
|---:|---:|---|:---:|---|---|---|
| 1 | 0 | 철심부 | `!` | 배전반 내 위치때문에 잘 안보임 | 부품 하나를 통째로 감싼다 | P1 |
| 2 | 1 | 에폭시 표면 | `O` | 가이드 비고 없음 — 기본 가공대상 | 부품 하나를 통째로 감싼다 | P1 |
| 3 | 2 | 몰드변압기 접촉부 | `O` | 가이드 비고 없음 — 기본 가공대상 | 볼트로 조여진 접속 지점 하나하나 | P1 |
| 4 | 3 | LBS | `O` | 가이드 비고 없음 — 기본 가공대상 | 부품 하나를 통째로 감싼다 | P2 |
| 5 | 4 | LBS 1차측 접촉부 | `O` | 가이드 비고 없음 — 기본 가공대상 | **미확정** | P2 |
| 6 | 5 | 한류형 전력퓨즈 | `O` | 가이드 비고 없음 — 기본 가공대상 | 부품 하나를 통째로 감싼다 | P2 |
| 7 | 6 | LA | `O` | 가이드 비고 없음 — 기본 가공대상 | 부품 하나를 통째로 감싼다 | P2 |
| 8 | 7 | 변압기 | `O` | 가이드 비고 없음 — 기본 가공대상 | 부품 하나를 통째로 감싼다 | P4 |
| 9 | 8 | 변류기 | `O` | 가이드 비고 없음 — 기본 가공대상 | 부품 하나를 통째로 감싼다 | P3 P4 |
| 10 | 9 | 비한류형 전력퓨즈 | `O` | 가이드 비고 없음 — 기본 가공대상 | 부품 하나를 통째로 감싼다 | P3 P4 |
| 11 | 10 | 소음기부착형 전력퓨즈 | `O` | 가이드 비고 없음 — 기본 가공대상 | 부품 하나를 통째로 감싼다 | P1 P5 |
| 12 | 11 | 몰드타입 PT | `O` | 가이드 비고 없음 — 기본 가공대상 | 부품 하나를 통째로 감싼다 | P4 P5 |
| 13 | 12 | 분기 접촉부 | `폐지` | [폐지] 새 가이드에서 PF&PT 반도 케이블헤드로 통일. 신규 라벨 금지 · 기존 실적 0건 · 라벨 승계 없음 (DEC-025) | — | — |
| 14 | 13 | 케이블헤드 | `O` | 가이드 비고 없음 — 기본 가공대상 | 부품 하나를 통째로 감싼다 | P2 P5 P6 |
| 15 | 14 | VCB 접촉부 | `O` | 가이드 비고 없음 — 기본 가공대상 | 볼트로 조여진 접속 지점 하나하나 | P6 P7 |
| 16 | 15 | CT | `O` | 가이드 비고 없음 — 기본 가공대상 | 부품 하나를 통째로 감싼다 | P6 P7 |
| 17 | 16 | SA | `O` | 가이드 비고 없음 — 기본 가공대상 | 부품 하나를 통째로 감싼다 | P6 |
| 18 | 17 | 콘덴서 | `O` | 가이드 비고 없음 — 기본 가공대상 | 부품 하나를 통째로 감싼다 | P8 P10 |
| 19 | 18 | MCCB | `O` | 가이드 비고 없음 — 기본 가공대상 | 부품 하나를 통째로 감싼다 | P8 P9 P10 |
| 20 | 19 | 인입선로 접촉부 | `O` | 가이드 비고 없음 — 기본 가공대상 | **미확정** | P5 |
| 21 | 20 | 부스바 | `X` | 일정한 패턴이 없어 탐지 정확도가 떨어짐 | — | — |
| 22 | 21 | 케이블 | `X` | 일정한 패턴이 없어 탐지 정확도가 떨어짐 | — | — |
| 23 | 22 | LBS 2차측 접촉부 | `O` | 가이드 비고 없음 — 기본 가공대상 | **미확정** | P2 |
| 24 | 23 | 변압기 접촉부 | `O` | 가이드 비고 없음 — 기본 가공대상 | **미확정** | P4 |
| 25 | 24 | 변류기 접촉부 | `O` | 가이드 비고 없음 — 기본 가공대상 | 볼트로 조여진 접속 지점 하나하나 | P3 P4 |
| 26 | 25 | CT 접촉부 | `!` | 배전반 내 위치때문에 잘 안보임 | **미확정** | P7 |
| 27 | 26 | ACB 접촉부 | `!` | 배전반 내 위치때문에 잘 안보임 | **미확정** | P8 P10 |
| 28 | 27 | MCCB 접촉부 | `!` | 다른 접촉부와 구분이 안됨 | 기기 한쪽 단자군 전체를 하나로 | P8 P9 P10 |

**클래스 28개** — 가공 21 · 주의 4 · 제외 2 · 폐지 1

**라벨 대상 25종 중 단위 확정 19종.** 미확정 6종은 라벨러에게 배포하지 않는다 (NQ-13).
